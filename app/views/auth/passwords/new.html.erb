<% provide :page_title, t('sessions.titles.forgot_password') %>
<% provide :title, t('sessions.titles.forgot_password') %>

<%= form_with url: passwords_path, html: { novalidate: true } do |form| %>
   <%= form.tw_email_field :email_address %>

   <%= form.tw_submit t('sessions.actions.send_reset_password_instructions'), class: 'w-full' %>
<% end %>

<div class="text-center mt-4">
   <%= link_to t('sessions.actions.back_to_sign_in'), new_session_path %>
</div>